<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.hotel.mapper.LwfPointlogMapper">

    <resultMap type="LwfPointlog" id="LwfPointlogResult">
        <result property="logId"       column="log_id"       />
        <result property="memberId"    column="member_id"    />
        <result property="type"        column="type"         />
        <result property="title"       column="title"        />
        <result property="points"      column="points"       />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"  column="create_time"  />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"  column="update_time"  />
        <result property="remark"      column="remark"       />
    </resultMap>

    <sql id="selectLwfPointlogVo">
        select log_id, member_id, type, title, points, create_by, create_time, update_by, update_time, remark from lwf_point_log
    </sql>

    <select id="selectLwfPointlogList" parameterType="LwfPointlog" resultMap="LwfPointlogResult">
        <include refid="selectLwfPointlogVo"/>
        <where>
            <if test="memberId != null"> and member_id = #{memberId}</if>
            <if test="type != null and type != ''"> and type = #{type}</if>
            <if test="title != null and title != ''"> and title like concat('%', #{title}, '%')</if>
        </where>
        order by create_time desc, log_id desc
    </select>

    <select id="selectLwfPointlogByLogId" parameterType="Long" resultMap="LwfPointlogResult">
        <include refid="selectLwfPointlogVo"/>
        where log_id = #{logId}
    </select>

    <insert id="insertLwfPointlog" parameterType="LwfPointlog" useGeneratedKeys="true" keyProperty="logId">
        insert into lwf_point_log
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="memberId != null">member_id,</if>
            <if test="type != null">type,</if>
            <if test="title != null">title,</if>
            <if test="points != null">points,</if>
            <if test="remark != null">remark,</if>
            <if test="createBy != null">create_by,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="memberId != null">#{memberId},</if>
            <if test="type != null">#{type},</if>
            <if test="title != null">#{title},</if>
            <if test="points != null">#{points},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null">#{createBy},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateLwfPointlog" parameterType="LwfPointlog">
        update lwf_point_log
        <trim prefix="SET" suffixOverrides=",">
            <if test="memberId != null">member_id = #{memberId},</if>
            <if test="type != null">type = #{type},</if>
            <if test="title != null">title = #{title},</if>
            <if test="points != null">points = #{points},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </trim>
        where log_id = #{logId}
    </update>

    <delete id="deleteLwfPointlogByLogId" parameterType="Long">
        delete from lwf_point_log where log_id = #{logId}
    </delete>

    <delete id="deleteLwfPointlogByLogIds" parameterType="String">
        delete from lwf_point_log where log_id in
        <foreach item="logId" collection="array" open="(" separator="," close=")">
            #{logId}
        </foreach>
    </delete>
</mapper>
