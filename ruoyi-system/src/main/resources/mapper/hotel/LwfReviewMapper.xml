<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.hotel.mapper.LwfReviewMapper">

    <resultMap type="LwfReview" id="LwfReviewResult">
        <result property="reviewId"    column="review_id"    />
        <result property="orderId"     column="order_id"     />
        <result property="memberId"    column="member_id"    />
        <result property="memberName"  column="member_name"  />
        <result property="orderTitle"  column="order_title"  />
        <result property="rating"      column="rating"       />
        <result property="content"     column="content"      />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"  column="create_time"  />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"  column="update_time"  />
        <result property="remark"      column="remark"       />
    </resultMap>

    <sql id="selectLwfReviewVo">
        select review_id, order_id, member_id, member_name, order_title, rating, content, create_by, create_time, update_by, update_time, remark from lwf_review
    </sql>

    <select id="selectLwfReviewList" parameterType="LwfReview" resultMap="LwfReviewResult">
        <include refid="selectLwfReviewVo"/>
        <where>
            <if test="orderId != null"> and order_id = #{orderId}</if>
            <if test="memberName != null and memberName != ''"> and member_name like concat('%', #{memberName}, '%')</if>
            <if test="rating != null"> and rating = #{rating}</if>
            <if test="content != null and content != ''"> and content like concat('%', #{content}, '%')</if>
        </where>
        order by review_id desc
    </select>

    <select id="selectLwfReviewByReviewId" parameterType="Long" resultMap="LwfReviewResult">
        <include refid="selectLwfReviewVo"/>
        where review_id = #{reviewId}
    </select>

    <insert id="insertLwfReview" parameterType="LwfReview" useGeneratedKeys="true" keyProperty="reviewId">
        insert into lwf_review
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="orderId != null">order_id,</if>
            <if test="memberId != null">member_id,</if>
            <if test="memberName != null">member_name,</if>
            <if test="orderTitle != null">order_title,</if>
            <if test="rating != null">rating,</if>
            <if test="content != null">content,</if>
            <if test="remark != null">remark,</if>
            <if test="createBy != null">create_by,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="orderId != null">#{orderId},</if>
            <if test="memberId != null">#{memberId},</if>
            <if test="memberName != null">#{memberName},</if>
            <if test="orderTitle != null">#{orderTitle},</if>
            <if test="rating != null">#{rating},</if>
            <if test="content != null">#{content},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null">#{createBy},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateLwfReview" parameterType="LwfReview">
        update lwf_review
        <trim prefix="SET" suffixOverrides=",">
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="memberId != null">member_id = #{memberId},</if>
            <if test="memberName != null">member_name = #{memberName},</if>
            <if test="orderTitle != null">order_title = #{orderTitle},</if>
            <if test="rating != null">rating = #{rating},</if>
            <if test="content != null">content = #{content},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </trim>
        where review_id = #{reviewId}
    </update>

    <delete id="deleteLwfReviewByReviewId" parameterType="Long">
        delete from lwf_review where review_id = #{reviewId}
    </delete>

    <delete id="deleteLwfReviewByReviewIds" parameterType="String">
        delete from lwf_review where review_id in
        <foreach item="reviewId" collection="array" open="(" separator="," close=")">
            #{reviewId}
        </foreach>
    </delete>
</mapper>
